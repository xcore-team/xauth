# XAuth — dépendances Python
# Généré depuis l'analyse des imports du plugin

# Web framework
fastapi>=0.111.0

# ORM asyncio
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
aiosqlite>=0.20.0

# Validation & sérialisation
pydantic[email]>=2.7.0

# JWT (RS256)
python-jose[cryptography]>=3.3.0

# HTTP client (OAuth providers)
httpx>=0.27.0

# Templating (emails)
jinja2>=3.1.4

# Hachage des mots de passe
passlib[argon2,bcrypt]>=1.7.4

# MFA TOTP
pyotp>=2.9.0
qrcode[pil]>=7.4.2
